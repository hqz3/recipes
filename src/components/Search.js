import React from "react";
import styled from "styled-components";

const StyledSearch = styled.section`
  border: 1px solid blue;
  width: 100%;
  text-align: center;
`;

const Search = () => {
  return (
    <>
      <StyledSearch>Search results...</StyledSearch>
    </>
  );
};

export default Search;
